""" 
tic tac toe board consists of 3 arrays with 3 elements inside each array.
array 1   [-,-,-],
array 2   [-,-,-],
array 3   [-,-,-]

check to see if the user input is => somthing 1-9, this accounts for all elements inside the arrays. 
if the user inputs a value that is not an X or an O there should be an error msg that tells the user to use the correct Board Piece. Also indicate what that piece is for each user. 
Check to see if a user has already placed a board piece in each location, add each user selectd place to the game board with an X or an O.
Check if the user has won by getting 3 in a row. Wins are in culmns and can be diagnoal too. Toggle between after each move
 """

 pygame cordinates

 x values increase in the right directiong
 y values increase as they go down which is uncommon. 